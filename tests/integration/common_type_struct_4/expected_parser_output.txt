proc foo'(T)(a: T, b: T) -> Unit
{
}

proc main() -> i32
{
    foo'(Option'(i32))(Option'(i32).None(), Option'(i32).Some(3));
    return 0;
}

struct Option'(T)
{
    case Some
    {
        value: T,
    },
    case None {},
}

typealias Some'(T) = Option'(T).Some;

typealias None'(T) = Option'(T).None;

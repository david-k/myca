struct Foo
{
    case A {},
    case B {},
    case C
    {
        case D {},
    },
}

proc f'(T)(a: T, b: T, c: T) -> Unit
{
}

proc main() -> i32
{
    f'(Foo)(Foo.A(), Foo.B(), Foo.C.D());
    return 0;
}

proc uninitialized'T() -> T {}

struct S'(T, let c: usize)
{
	arr: [c]T,
}

proc main() -> i32
{
	let arr: [2]i8 = uninitialized();
	let mut s = S(arr);
	s.arr[0] := 2;
	s.arr[1] := 3;
	return s.arr[0] + s.arr[1];
}
